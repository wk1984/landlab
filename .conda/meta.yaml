package:
  name: landlab
  version: "1.0.0beta"

source:
  git_url: .
  # git_url: https://github.com/landlab/landlab

requirements:
  build:
    - python =={{environ.get('PYTHON_VERSION', '2.7')}}
    - scipy
    - numpy =={{environ.get('NUMPY_VERSION', '1.10')}}
    - nose
    - matplotlib
    - numpydoc
    - netcdf4
    - sympy
    - pandas
    - cython
    - six
    - pyyaml
    - setuptools

  run:
    - python =={{environ.get('PYTHON_VERSION', '2.7')}}
    - scipy
    - numpy =={{environ.get('NUMPY_VERSION', '1.10')}}
    - nose
    - matplotlib
    - numpydoc
    - netcdf4
    - sympy
    - pandas
    - cython
    - six
    - pyyaml
    - setuptools

build:
  string: {{ environ.get('BUILD_STR', '') }}
